/** Invariant companion plugin (no assertions — nothing to check at runtime). */

/** Provides no assertions: the winrm plugin owns no cross-package runtime invariants. */
export function apply(): void {}
