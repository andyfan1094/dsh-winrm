# 安全说明 / Security notes

- 主机密码明文保存在本机 `~/.dsh/dsh-winrm.json`（权限 0600，仅当前用户可读），界面永不回显。该文件是本机配置而非加密保险库，请用专用低权限账户管理远程主机。
- 密码通过 stdin 传给 pywinrm bridge，不出现在进程命令行参数中。
- HTTP(5985) + Basic 认证是明文传输，仅限受信内网；跨网段或公网必须使用 HTTPS(5986)，自签名证书可在插件中显式勾选接受。
- 远程命令以所配置账户的完整权限执行；`winrm_cluster` 会并发操作多台主机，执行前先确认 aliases / environment / tags 过滤范围。
- 控制台会话与命令执行都受单命令超时（默认 60s）约束，长任务请自行评估影响。

发现问题请通过 GitHub issues 反馈；不要在 issue 中粘贴主机地址、账户名或密码。
